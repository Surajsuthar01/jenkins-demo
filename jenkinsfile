pipeline {
    agent any

    stages {

        stage('Clone') {
            steps {
                checkout scm
            }
        }

        stage('Deploy to Apache') {
            steps {
                sh '''
                 rsync -av --delete ./ /var/www/html/
                '''
            }
        }
    }
}
